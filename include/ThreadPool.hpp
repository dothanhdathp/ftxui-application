/**
 * Hi, I'm Tad
 * I think i will create an thread pools to supported gendering task, control task id, can stop thread on UI,
 * But then i remmember i not good enought set ftxui re-draw screen so it will be push in to do
 */